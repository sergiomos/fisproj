const result = ({ label, onChange, ...props }) => (
  <li className='flex justify-between'>
    <span>
      Função de onda quântica Nf:
    </span>
    <span className='font-bold'>ψ(x) = {result.a} * sen({result.kNf}*x)
    </span>
  </li>
)
